{% extends 'base.html.twig' %}

{% block title %}AlertUser{% endblock %}

{% block body %}
    <h1>AlertUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alert_user.id }}</td>
            </tr>
            <tr>
                <th>MaxPrice</th>
                <td>{{ alert_user.maxPrice }}</td>
            </tr>
            <tr>
                <th>PostalCode</th>
                <td>{{ alert_user.postalCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('alert_user_index') }}">back to list</a>

    <a href="{{ path('alert_user_edit', {'id': alert_user.id}) }}">edit</a>

    {{ include('alert_user/_delete_form.html.twig') }}
{% endblock %}
