{% extends 'base.html.twig' %}

{% block title %}Edit AlertUser{% endblock %}

{% block body %}
    <h1>Edit AlertUser</h1>

    {{ form_start(form) }}
        {{ form_row(form.maxPrice) }}
        {{ form_row(form.postalCode) }}
        {{ form_row(form.idActivity) }}
        <button class="btn btn-sm btn-primary">Editer</button>
    {{ form_end(form) }}

    <a href="{{ path('alert_user_index') }}">back to list</a>

{% endblock %}
