{% extends 'base.html.twig' %}

{% block title %}

{% endblock %}

{% block body %}
    <div class="container">
        <h2>Bienvenu sur votre compte {{ user.username }}</h2>
        <div class="row">
            <div class="col-md-8">
                <h4>Biens ajoutés</h4>
                <ul>
                    {% for listProperty in user.properties %}
                        <a href="{{ path('bien_show', { id: listProperty.bien.id, slug : listProperty.bien.slug}) }}"><li>{{ listProperty.bien.titreannonce }}</li></a>
                    {% endfor %}
                </ul>
            </div>
            <div class="col-md-4">
                <h4>Nom du compte</h4>
                <p>{{ user.username }}</p>
            </div>
        </div>
    </div>

{% endblock %}
